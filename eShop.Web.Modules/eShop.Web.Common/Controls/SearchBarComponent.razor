<form class="form-inline">
    <div class="form-group mx-sm-3 mb-2">
        <label for="filter">Search:&nbsp</label>
        <input type="text" class="form-control" id="filter" placeholder="Enter search values" @bind-value="filter">
    </div>
    <button type="button" class="btn btn-primary mb-2" @onclick="HandleSearch">Search</button>
</form>
@code {

    // Allows child component to talk to parent page/component.
    [Parameter]
    public EventCallback<string> OnSearch { get; set; }

    private string filter;

    private void HandleSearch()
    {
        OnSearch.InvokeAsync(filter);
    }
}
